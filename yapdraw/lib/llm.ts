import { ExcalidrawElement } from '@/types/diagram'

export async function generateDiagram(
  _transcript: string,
  _currentElements: ExcalidrawElement[]
): Promise<ExcalidrawElement[]> {
  return []
}
